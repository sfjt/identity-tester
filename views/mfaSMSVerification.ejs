<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/public/favicon.ico">
    <style>
      dt {
        font-weight: bold;
      }
      nav {
        margin-bottom: 2em;
        border-width: 1px 0;
        border-style: dotted;
        border-color: gray;
      }
    </style>
    <title>SMS MFA Enrollment</title>
  </head>
  <body>
    <h1>SMS MFA Enrollment</h1>
    <nav>
      <ul>
        <li><a href="/mfa">MFA Settings</a></li>
      </ul>
    </nav>
    <p>A verification code has been sent to your phone: <%= phoneNumber %> </p>
    <% if(verificationResult) { %>
      <p><strong>Verification Result: Successful</strong></p>
    <% } else { %>
      <section>
        <form action="/mfa/sms/verify" method="POST">
          <p>oob_code: <input value="<%= oobCode %>" name="oob_code"></p>
          <p>binding_code (verification code): <input value="" name="binding_code"></p>
          <input type="hidden" value="<%= phoneNumber %>" name="phone_number">
          <p><button>Submit</button></p>
        </form>
      </section>
    <% } %>
  </body>
</html>
